import React from "react";
import Link from "next/link";

const Home = () => (
  <div>
    <Link href="/user">Submit! (but doesn't actually do anything really)</Link>
  </div>
);

export default Home;
